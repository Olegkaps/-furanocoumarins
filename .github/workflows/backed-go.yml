# This workflow will build a golang project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-go

name: Go test and build

on:
  push:
    branches: [ "main" ]
    paths:
    - "backend/admin/**"
    - "docker-compose.test.yaml"
    - ".github/workflows/backed-go.yml"

  pull_request:
    branches: [ "main" ]
    paths:
    - "backend/admin/**"
    - "docker-compose.test.yaml"
    - ".github/workflows/backed-go.yml"

jobs:
  golangci:
    name: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: backend/admin/go.mod
          cache-dependency-path: backend/admin/go.sum
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.10
          working-directory: backend/admin

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version-file: backend/admin/go.mod
        cache-dependency-path: backend/admin/go.sum

    - name: Build
      run: cd backend/admin && go build -v ./...

    - name: Test
      run: make test-backend-container COMPOSE='docker compose'

    - name: Build the Docker image
      run: docker build ./backend/admin --tag back-app
